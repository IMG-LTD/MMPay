# Architecture Overview

MMPay is planned as a payment gateway with separate modules for core payment
state, provider adapters, outbound webhooks, admin APIs, and deployment entry
points.

The MP-0 repository contains only governance and validation scaffolding.
