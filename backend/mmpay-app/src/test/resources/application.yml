spring:
  datasource:
    hikari:
      connection-init-sql:
  flyway:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,env,configprops,metrics
  endpoint:
    health:
      probes:
        enabled: true
    info:
      enabled: true
