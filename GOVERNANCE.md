# Governance

MMPay is maintained as an independent IMG-LTD repository. It must not be nested
inside the MMMail source tree or treated as a MMMail module.

The project accepts changes only when they keep payment failures explicit,
avoid mock success paths, and preserve the license signing boundary documented
in `AGENTS.md`.
